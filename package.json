{
  "name": "eze3",
  "version": "1.1.0",
  "description": "NYCU Portal Autofill Chrome Extension",
  "private": true,
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist && cd src && zip -r ../dist/EZE3.zip . -x '*.DS_Store'",
    "release": "release-it --config release-it.config.cjs"
  },
  "devDependencies": {
    "release-it": "^20.2.0"
  }
}
